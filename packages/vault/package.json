{
  "name": "@attestia/vault",
  "version": "0.1.0",
  "description": "Personal Vault — multi-chain observation, envelope budgeting, intent-based allocation",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@attestia/chain-observer": "workspace:*",
    "@attestia/ledger": "workspace:*",
    "@attestia/registrum": "workspace:*",
    "@attestia/types": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
