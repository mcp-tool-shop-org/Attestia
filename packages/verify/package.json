{
  "name": "@attestia/verify",
  "version": "0.2.0",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "description": "Deterministic replay verification and GlobalStateHash for Attestia",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "bench": "vitest bench",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@attestia/ledger": "workspace:*",
    "@attestia/registrum": "workspace:*",
    "@attestia/types": "workspace:*",
    "json-canonicalize": "^2.0.0"
  },
  "devDependencies": {
    "@attestia/witness": "workspace:*",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
